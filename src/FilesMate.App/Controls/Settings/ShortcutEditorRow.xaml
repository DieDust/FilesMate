<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FilesMate.App.Controls.Settings.ShortcutEditorRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid MinHeight="64">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Padding="16,10" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="TitleText"
                VerticalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Text="Action"
                TextWrapping="Wrap" />
            <Button
                x:Name="GestureButton"
                Grid.Column="1"
                MinWidth="126"
                Padding="14,7"
                HorizontalContentAlignment="Center"
                Click="GestureButton_Click"
                KeyDown="GestureButton_KeyDown"
                LostFocus="GestureButton_LostFocus"
                Style="{StaticResource GlassButtonStyle}" />
        </Grid>
        <TextBlock
            x:Name="ErrorText"
            Grid.Row="1"
            Margin="16,-4,16,8"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            FontSize="12"
            TextWrapping="Wrap"
            Visibility="Collapsed" />
        <Border
            x:Name="Divider"
            Grid.Row="2"
            Height="1"
            Margin="16,0"
            Background="{ThemeResource FilesMate.Divider.Brush}" />
    </Grid>
</UserControl>
